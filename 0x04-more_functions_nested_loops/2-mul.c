#include "main.h"
/**
 * mul - Multiplies two integers
 * @x: Integer to multiply
 * @y: Integer to multiply
 * Return: The result after multiplication of the two integers
 */
int mul(int x, int y)
{
	return (x * y);
}

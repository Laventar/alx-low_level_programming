#include "main.h"
/**
 * _isupper - Check for uppercase letter
 * @x: The number to be checked
 * Return: 1 for uppercase letter or else 0
 */
int _isupper(int x)
{
	if (x >= 65 && x <= 90)
	{
	return (1);
	}
	return (0);
}
